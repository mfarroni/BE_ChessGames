# Connection URL for PostgreSQL Database (e.g. hosted on Render or Supabase)
DATABASE_URL=

# Port on which the express server runs (defaults to 3000 if not specified)
PORT=3000

# SMTP mail server credentials for real-time verification email delivery
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Circolo degli Scacchi" <no-reply@circoloscacchi.it>
